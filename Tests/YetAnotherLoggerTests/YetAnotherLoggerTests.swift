import XCTest
@testable import YetAnotherLogger

final class YetAnotherLoggerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(YetAnotherLogger().text, "Hello, World!")
    }
}
